let array = ['pen', 'tiffinbox', 'pencil', 'book', 'door', 'bag', 'scale']

// array.forEach(value => console.log(value.toUpperCase()))

// return in an array

let arr2 = [];

array.forEach(value => {
    arr2.push(value.toUpperCase());
})
